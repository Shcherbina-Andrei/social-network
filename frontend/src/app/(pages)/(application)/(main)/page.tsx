import { ProfileInfo } from '@/widgets/profile-info';

export default function Profile() {
  return (
    <div>
      <section>
        <ProfileInfo />
      </section>
      <section>Posts</section>
    </div>
  );
}
